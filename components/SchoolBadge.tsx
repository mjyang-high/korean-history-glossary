export function SchoolBadge({ className = '' }: { className?: string }) {
  return (
    <div className={`font-display text-sm font-bold text-[#1c1a16]/80 ${className}`}>
      분당대진고등학교 양민정
    </div>
  );
}
