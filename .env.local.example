# Claude API 키 (https://console.anthropic.com 에서 발급)
ANTHROPIC_API_KEY=sk-ant-...

# 반 전체가 공유하는 접속 비밀번호
CLASS_PASSWORD=바꿔주세요

# Vercel Postgres 연결 문자열
# Vercel 대시보드 > 프로젝트 > Storage > Postgres 생성 후 발급되는 값을 그대로 복사
# (vercel env pull .env.local 명령으로 자동으로 받아올 수도 있음)
POSTGRES_URL=postgres://...
